
// Import from the server directory
// Note: Relative path depends on where 'project/api' is relative to 'project/server'
// 'project/api/index.ts' -> '../server/src/index'
import app from '../server/src/index';

export default app;
